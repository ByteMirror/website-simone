/**
 * Feature Toggles
 *
 * Control various features of the website.
 * Set to `true` to enable, `false` to disable.
 */

export const features = {
  /**
   * Password Protection
   *
   * When enabled, visitors must enter the correct password
   * to view the website. Useful during development or for
   * previewing with clients before public launch.
   */
  passwordProtection: true,
}
